import kotlin.test.Test
import kotlin.test.assertEquals

class GenerateLengthTest {

    @Test
    fun `check the length and uppercase length`() {
       val length = 10

        var digit = 2

        val newLength = length + digit
        val newLength2 = length - digit
    }
}